<h1><%= @student.to_s %></h1>
<%# <p><%= @student.active ? "This student is currently active." : "This student is currently inactive." %></p> %>
<% if @student.active %>
  This student is currently active.
<% else %>
  This student is currently inactive.
<% end %>